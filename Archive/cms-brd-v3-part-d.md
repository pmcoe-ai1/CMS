### Part D: Search Optimization

This part covers both traditional search engine optimization and AI search optimization. Many requirements in this part are system-level automations that run without direct user interaction. For these, the columns note that the system executes automatically and describe how the user can review, override, or monitor the results through each interface.

### 5.13 On-Page SEO

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.13.1 | Every page must automatically have a meta title generated based on page content, with the primary keyword placed within the first 60 characters. Titles must be kept under 60 characters to prevent truncation. The system must allow users to override auto-generated titles. | User opens the SEO settings panel for a page. The auto-generated meta title is displayed with a character counter. User edits the field to override. A warning appears if the title exceeds 60 characters. | User says "show me the meta title for the homepage" and the system displays it. User says "change the meta title to 'Professional Certification Courses — Acme Training'" and the system updates it on the SEO panel. User says "which pages have meta titles over 60 characters?" and the system lists them. |
| 5.13.2 | Every page must automatically have a meta description generated based on page content, kept under 155–160 characters, written as a compelling summary with a clear value proposition. The system must allow users to override. | User opens the SEO settings panel. The auto-generated meta description is displayed with a character counter. User edits the field to override. | User says "show me the meta description for this page" and the system displays it. User says "rewrite the meta description to focus on our pricing advantage" and the system generates a new description for review. |
| 5.13.3 | Every page must automatically have Open Graph tags (og:title, og:description, og:image, og:url, og:type) generated for social media sharing. | User opens the social sharing preview panel within the SEO settings. Previews show how the page will appear when shared on Facebook, X/Twitter, and LinkedIn. User can override any OG field. | User says "show me the social sharing preview for this page" and the system displays it. User says "change the OG image to our new banner" and the system updates it. |
| 5.13.4 | The system must automatically generate clean, descriptive, human-readable URLs based on page titles, using hyphens as word separators, lowercase only, and avoiding parameter strings, filler words, and excessive depth. | User sees the auto-generated URL slug in the page settings panel. User can edit the slug directly. Validation warns about invalid characters or duplicates. | User says "what's the URL for this page?" and the system displays it. User says "change the URL to /certification-courses/" and the system updates the slug with a redirect from the old URL. |
| 5.13.5 | For any new page or any page ported from an existing website, the system must allow the user to specify a custom URL. If not specified, the system auto-generates based on the page title. When porting pages, the system must preserve the original URL structure by default, with the option to override. | User opens page settings and edits the URL slug field. During site import/cloning, a URL mapping panel shows original URLs alongside proposed new URLs. User overrides individually or in bulk. | User says "set the URL for this page to /training/pmp-sydney" and the system applies it. User says "keep all original URLs when importing the site" and the system preserves them. User says "show me all URL mappings from the import" and the system displays the list. |
| 5.13.6 | All pages must use semantic HTML with proper heading hierarchy (single H1 per page containing the primary topic, H2s for major sections, H3s for subsections). | The design canvas enforces heading hierarchy by default. The heading level selector in the formatting toolbar shows H1 (one per page), H2, H3, etc. An SEO audit panel flags pages with missing or duplicate H1 tags. | User says "check the heading structure of this page" and the system displays the heading hierarchy. User says "fix the duplicate H1 on the about page" and the system resolves it on the canvas. |
| 5.13.7 | The system must use semantic HTML5 elements (article, section, nav, aside, main, header, footer) to support search engine content understanding and accessibility. | System-level — the system automatically generates semantic HTML for all page structures. User can inspect the HTML output through a code view panel. | System-level — automatic. User says "show me the HTML structure of this page" and the system displays the semantic element outline. |
| 5.13.8 | The system must support hreflang tags for pages that have regional or language variants, enabling proper geo-targeting for multi-market websites. | User opens the language/region settings for a page. Links language variants together through a variant selector. The system auto-generates hreflang tags for all linked variants. | User says "link the French version of this page to the English version" and the system creates the hreflang relationship. User says "show me all pages missing hreflang tags" and the system lists them. |
| 5.13.9 | The system must automatically generate canonical URLs for all pages to prevent duplicate content issues. When multiple URLs serve similar content, the system must designate and enforce the canonical version. | User views the canonical URL in the SEO settings panel. User can override the auto-assigned canonical if needed. | User says "what's the canonical URL for this page?" and the system displays it. User says "set the canonical for /services/ to /our-services/" and the system applies it. |
| 5.13.10 | The system must automatically optimise images: compression to WebP or AVIF format, lazy loading for images below the fold, responsive sizing via srcset, explicit width and height attributes to prevent Cumulative Layout Shift, and descriptive file names and alt text. | System-level — images are automatically optimised on upload. User sees optimised versions in the media library with file size comparisons. User edits alt text through the image properties panel on the canvas. | User says "optimise all images on this page" and the system confirms they are already optimised (or processes any that aren't). User says "show me images missing alt text" and the system lists them. User says "set the alt text for the hero image to 'Professional training classroom'" and the system updates it. |
| 5.13.11 | The system must support internal linking management, ensuring every important page is reachable within 3–4 clicks from the homepage. The system must identify and alert users to orphan pages (pages with no internal links pointing to them). | User opens the internal linking report in the SEO dashboard. A visual site map shows link depth. Orphan pages are highlighted with a warning indicator. User clicks an orphan page to add links to it. | User says "show me orphan pages" and the system lists them. User says "which pages are more than 4 clicks from the homepage?" and the system displays them. User says "suggest internal links for the new blog post" and the system recommends relevant pages to link to. |
| 5.13.12 | The system must ensure the primary keyword or topic phrase appears naturally within the first 100–150 words of page content. | The SEO settings panel shows a keyword placement indicator for each page, showing whether the primary keyword appears in the first 150 words. | User says "does the primary keyword appear early enough on this page?" and the system analyses the content and reports. User says "check keyword placement across all service pages" and the system generates a report. |
| 5.13.13 | The system must support and encourage topic cluster content architecture (hub-and-spoke model): hub pages providing comprehensive overviews linked to detailed spoke pages covering subtopics, with cross-linking between related spokes. | User opens the topic cluster manager in the SEO dashboard. Creates hub pages and links spoke pages to them. A visual diagram shows the cluster structure with link status. | User says "create a topic cluster around 'project management certification'" and the system suggests a hub page and spoke topics. User says "show me the topic cluster structure for our training pages" and the system displays the diagram. |
| 5.13.14 | The system must not apply keyword stuffing or keyword density optimisation. Content must use keywords naturally. | The SEO audit flags pages where keyword density exceeds natural thresholds with a warning (not a target to meet). No keyword density metric is presented as a goal. | If a user says "increase the keyword density on this page" the system explains that keyword stuffing harms both traditional and AI search rankings and suggests natural content improvements instead. |

### 5.14 Technical SEO

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.14.1 | The system must automatically generate and maintain an XML sitemap that includes all published pages, is updated whenever pages are created, modified, or deleted, uses accurate lastmod dates, and stays under 50,000 URLs / 50MB per file. The sitemap must be submitted to Google Search Console. | User opens the sitemap panel in the SEO dashboard. Views the current sitemap with page count, last updated date, and submission status. User can force a regeneration or manually submit to GSC. | User says "show me the sitemap status" and the system displays page count, last update, and GSC submission status. User says "regenerate the sitemap" and the system rebuilds and resubmits it. |
| 5.14.2 | The system must automatically generate and maintain a robots.txt file that allows all major search engine crawlers (Googlebot, Googlebot-Smartphone, Bingbot) access to public content, does not block CSS/JS/image resources needed for rendering, and blocks only administrative/private areas. | User opens the robots.txt editor in the SEO dashboard. Views the current file with syntax highlighting. User can edit rules directly. A validation indicator shows any errors. | User says "show me our robots.txt" and the system displays it. User says "are we blocking any resources that Google needs?" and the system analyses and reports. |
| 5.14.3 | The system must ensure all critical page content is rendered server-side (SSR) or pre-rendered so that search engine crawlers that do not execute JavaScript can access the full content. | System-level — the system uses SSR/pre-rendering by default. User can verify by viewing the rendered HTML source through a "View as Crawler" panel. | User says "is this page server-side rendered?" and the system confirms. User says "show me how Googlebot sees this page" and the system displays the crawler-rendered version. |
| 5.14.4 | The system must support 301 redirects so that when a page URL is changed or a page is deleted, traffic to the old URL is automatically redirected to the appropriate destination. Redirect chains must be minimised. | 1) When a user changes a URL, the system automatically creates a 301 redirect and shows a confirmation.<br>2) User opens the redirect manager to view, edit, or delete redirects.<br>3) A redirect chain detector flags chains longer than 2 hops. | User says "show me all active redirects" and the system displays the list. User says "create a redirect from /old-page to /new-page" and the system applies it. User says "are there any redirect chains?" and the system reports. |
| 5.14.5 | The system must serve all pages over HTTPS with valid SSL/TLS certificates. HTTPS is a confirmed Google ranking signal. Mixed content (HTTP resources on HTTPS pages) must be prevented. | System-level — HTTPS is enforced by default. A security panel shows certificate status, expiry date, and mixed content scan results. | User says "is our SSL certificate valid?" and the system reports status and expiry. User says "scan for mixed content" and the system checks all pages and reports any HTTP resources. |
| 5.14.6 | The system must meet or exceed Google Core Web Vitals thresholds on all pages: Largest Contentful Paint (LCP) under 2.5 seconds, Interaction to Next Paint (INP) under 200 milliseconds, Cumulative Layout Shift (CLS) under 0.1. | User opens the Core Web Vitals panel in the SEO dashboard. Each page shows LCP, INP, and CLS scores with pass/fail indicators. Pages failing any threshold are highlighted. | User says "show me Core Web Vitals for the homepage" and the system displays the scores. User says "which pages are failing CWV?" and the system lists them with the specific metrics that fail. |
| 5.14.7 | The system must support mobile-first design as the default. Google uses mobile-first indexing — the mobile version of pages is what gets crawled and ranked. All pages must render correctly and perform well on mobile devices. | System-level — mobile-first rendering is the default. User can verify through the responsive preview (per 5.5.6) and a mobile-friendly test panel in the SEO dashboard. | User says "does this page pass Google's mobile-friendly test?" and the system analyses and reports. User says "which pages have mobile rendering issues?" and the system lists them. |
| 5.14.8 | The system must manage crawl budget efficiently by preventing the generation of duplicate pages, parameter URL variations, and redirect chains. | The SEO dashboard includes a crawl efficiency report showing duplicate URLs detected, parameter variations, redirect chains, and crawl waste indicators. | User says "show me our crawl efficiency report" and the system displays it. User says "are we wasting crawl budget anywhere?" and the system identifies issues. |
| 5.14.9 | The system must automatically detect and prevent duplicate content issues, including URL variations (with/without trailing slash, with/without www, HTTP/HTTPS, parameter variations) through proper canonical tags and URL normalisation. | System-level — URL normalisation is automatic. The SEO dashboard shows a duplicate content report listing any detected issues with resolution status. | User says "do we have any duplicate content issues?" and the system analyses and reports. User says "normalise all URLs to use trailing slashes" and the system applies the rule site-wide. |
| 5.14.10 | The system must generate and maintain an index coverage health report, identifying pages excluded from search engine indexes and the reasons for exclusion (noindex, canonicalised, crawl errors, thin content). | User opens the index coverage report in the SEO dashboard. Pages are categorised as indexed, excluded (with reason), or errored. Data is pulled from Google Search Console integration. | User says "show me our index coverage report" and the system displays it. User says "which pages are excluded from Google's index and why?" and the system lists them with reasons. |

### 5.15 Structured Data

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.15.1 | Every page must automatically include appropriate structured data (JSON-LD schema markup) without manual intervention. The system must detect the page type and generate the correct schema. | System-level — schema is auto-generated on page creation and update. User opens the structured data panel for any page to view the generated JSON-LD with a rendered preview of how rich results will appear. | User says "show me the schema markup for this page" and the system displays the JSON-LD. User says "what rich results will this page generate?" and the system shows a preview. |
| 5.15.2 | The system must support and auto-generate the following schema types based on page content: Organization, LocalBusiness, WebSite, Product, Offer, Course, CourseInstance, Event, Article, BlogPosting, FAQPage, HowTo, Person, AggregateRating, Review, BreadcrumbList, VideoObject, and Speakable. | The structured data panel shows which schema types are applied to the current page. User can add, remove, or override schema types through a schema type selector. | User says "what schema types are on this page?" and the system lists them. User says "add Event schema to this page" and the system generates it based on the page content. |
| 5.15.3 | The system must automatically generate breadcrumb navigation and corresponding BreadcrumbList schema for all pages based on the site hierarchy. | System-level — breadcrumbs are auto-generated. User sees breadcrumbs on the canvas and can customise the hierarchy in the page settings panel. | User says "show me the breadcrumb path for this page" and the system displays it. User says "change the breadcrumb parent to 'Training' instead of 'Services'" and the system updates the hierarchy. |
| 5.15.4 | The system must support AggregateRating and Review schema, with the ability to connect to external review sources or accept manually entered review data. | User opens the reviews panel in page settings. Connects an external review source (Google Reviews, Trustpilot) or manually enters review data. The system generates corresponding schema. | User says "connect our Google Reviews to this page" and the system opens the integration setup. User says "add a review with 5 stars from 'John Smith' with the text 'Excellent course'" and the system creates the review entry and schema. |
| 5.15.5 | When content is updated on any page, the system must automatically update all associated schema markup to stay in sync. Schema must never contain stale data (prices, dates, availability). | System-level — schema updates automatically on content save. A freshness indicator in the structured data panel shows whether schema matches current content. | System-level — automatic. User says "is the schema on the pricing page up to date?" and the system verifies and reports. |
| 5.15.6 | The system must validate all generated schema markup and alert the user to any errors or warnings. Validation must align with Google's Rich Results Test requirements. | The structured data panel shows validation status for each page (pass/warning/error). A site-wide schema validation report is available in the SEO dashboard. | User says "validate the schema on this page" and the system runs validation and reports results. User says "show me all pages with schema errors" and the system lists them. |

### 5.16 Content Quality & E-E-A-T

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.16.1 | The system must support and encourage the creation of comprehensive, in-depth content that thoroughly addresses user search intent. | The SEO audit panel includes a content depth indicator per page, flagging thin content (below configurable word count thresholds) and suggesting areas for expansion. | User says "is this page comprehensive enough for SEO?" and the system analyses content depth, topical coverage, and search intent alignment. User says "what topics should I cover to make this page more comprehensive?" and the system suggests subtopics. |
| 5.16.2 | The system must support author/contributor profiles with visible credentials, experience, and expertise on every authored page. | User opens the author profile manager. Creates author profiles with name, photo, bio, credentials, and social links. Assigns authors to pages through the page settings panel. Author bios render on published pages. | User says "create an author profile for Sarah Jones with the title 'Senior Training Consultant'" and the system creates it. User says "assign Sarah as the author of all training pages" and the system applies the attribution. |
| 5.16.3 | The system must support and display trust signals: visible business information (address, contact details, about page), clear policies (privacy, returns, terms of service), security indicators (HTTPS, trust badges), and verifiable credentials (certifications, awards, accreditations). | User configures trust signals through the site settings panel: business information, policy pages, and trust badges. The system generates appropriate schema (Organization, LocalBusiness) and displays indicators on published pages. | User says "add our business address to the footer and schema" and the system updates both. User says "are we displaying all recommended trust signals?" and the system audits and reports any gaps. |
| 5.16.4 | The system must support content freshness management: visible "last updated" dates on all pages, automated staleness alerts when content has not been refreshed within a configurable period, and easy content update workflows. | 1) Published pages display "Last updated" dates automatically.<br>2) The content dashboard highlights stale pages past the configured threshold (default: 90 days).<br>3) User clicks a stale page to open it for editing. | User says "show me pages that haven't been updated in 90 days" and the system lists them. User says "set the staleness threshold to 60 days" and the system updates the policy. User says "what's the freshness status across the site?" and the system provides a summary. |
| 5.16.5 | The system must support multimedia content (images, videos, infographics, embedded media) on all pages. Video content must support VideoObject schema for discoverability. | User adds multimedia through the design canvas — dragging media from the library or embedding URLs. Video embeds automatically generate VideoObject schema. The media panel shows all multimedia assets on the page. | User says "add a video embed from this YouTube URL to the hero section" and the system places it on the canvas with auto-generated VideoObject schema. User says "which pages have no multimedia?" and the system lists them. |
| 5.16.6 | The system must support content organised by search intent type, recognising and optimising differently for informational pages (guides, how-tos, FAQs), commercial pages (comparisons, reviews), transactional pages (product/service pages, pricing), and navigational pages (brand/feature pages). | The page settings panel includes a "Search Intent" selector with options: Informational, Commercial, Transactional, Navigational. The system adjusts SEO recommendations based on the selected intent type. | User says "set this page's search intent to Transactional" and the system applies it. User says "show me all informational pages" and the system filters the content list. User says "what search intent should this page target?" and the system analyses the content and recommends. |

### 5.17 AI Search Optimization

#### 5.17.1 AI-Optimized Structured Data

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.17.1.1 | Every page must automatically include AI-optimised structured data beyond traditional SEO schema. The system must generate schema optimised for both rich results (traditional SEO) and AI citation (AI search) simultaneously. | The structured data panel (per 5.15) includes an "AI Optimisation" tab showing additional AI-specific schema attributes and their status per page. | User says "is this page optimised for AI citation?" and the system analyses the schema and reports. User says "show me the AI-specific schema for this page" and the system displays it alongside the traditional schema. |
| 5.17.1.2 | When content is updated on any page, the system must automatically update all associated schema markup — both traditional SEO and AI-optimised — to stay in sync. Schema must never contain stale data. | System-level — automatic on content save. The structured data panel shows a sync status indicator for both traditional and AI schema. | System-level — automatic. User says "is all schema in sync with current content?" and the system verifies across all pages and reports any mismatches. |
| 5.17.1.3 | The system must validate all generated schema markup against both Google's Rich Results Test requirements and AI extractability standards, and alert the user to any errors or warnings. | The schema validation report (per 5.15.6) includes an AI extractability score alongside the traditional validation status. | User says "validate schema for both Google and AI extractability" and the system runs both validations and reports combined results. |

#### 5.17.2 AI Crawler Access

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.17.2.1 | The system must extend the robots.txt file to explicitly allow all major AI crawlers: GPTBot, ChatGPT-User, ClaudeBot, PerplexityBot, Google-Extended, Bytespider, CCBot, Applebot-Extended, cohere-ai, and anthropic-ai. | User opens the robots.txt editor (per 5.14.2). AI crawler rules are displayed alongside traditional crawler rules. User can enable or disable individual AI crawlers. | User says "which AI crawlers are allowed?" and the system lists them. User says "block Bytespider" and the system updates robots.txt. User says "allow all AI crawlers" and the system ensures all are permitted. |
| 5.17.2.2 | The system must automatically generate and maintain an llms.txt file (per the llms.txt specification) that provides AI systems with a markdown-formatted, curated index of the website's key content, including page descriptions and links. | User opens the llms.txt panel in the SEO dashboard. Views the generated file with page listings. User can edit descriptions or exclude specific pages. | User says "show me our llms.txt file" and the system displays it. User says "exclude the internal team page from llms.txt" and the system removes it. |
| 5.17.2.3 | The system must automatically generate markdown versions of all published pages (accessible at the same URL with .md extension) to support AI content consumption per the llms.txt specification. | System-level — markdown versions are auto-generated. User can preview the markdown version of any page through a "View as Markdown" option in the page toolbar. | User says "show me the markdown version of this page" and the system displays it. User says "are all pages generating markdown versions?" and the system reports coverage. |
| 5.17.2.4 | The system must automatically keep robots.txt, llms.txt, and markdown page versions in sync with content changes. When pages are created, modified, or deleted, these files must update automatically. | System-level — sync is automatic. The AI crawler access panel shows last-sync timestamps for each file. A warning appears if any file is out of sync. | System-level — automatic. User says "when was llms.txt last updated?" and the system reports. User says "force a sync of all AI access files" and the system refreshes them. |

#### 5.17.3 AI Content Structure

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.17.3.1 | The system must automatically structure page content for AI extractability. Each major content section must open with a concise 40–60 word answer block that summarises the key facts of that section. | The AI content structure panel shows each page section with its answer block status (present/missing/too long). User can edit answer blocks inline. | User says "show me the answer blocks on this page" and the system highlights them. User says "generate an answer block for the pricing section" and the system creates a 40–60 word summary for review. |
| 5.17.3.2 | The system must format FAQ sections using question-and-answer pairs with corresponding FAQPage schema markup, enabling direct extraction by AI answer engines and voice assistants. | User adds an FAQ component on the canvas. The system automatically generates FAQPage schema for each Q&A pair. The structured data panel shows the FAQ schema. | User says "convert this section into an FAQ format" and the system restructures the content into Q&A pairs on the canvas with auto-generated schema. |
| 5.17.3.3 | The system must use question-based headings (H2/H3) that match natural language queries users ask AI platforms, rather than generic section titles. | The AI content audit panel flags generic headings (e.g., "Shipping Information") and suggests question-based alternatives (e.g., "How long does shipping take?"). | User says "suggest question-based headings for this page" and the system analyses the content and proposes alternatives. User says "convert all headings on this page to question format" and the system rewrites them for review. |
| 5.17.3.4 | The system must format comparison content as HTML tables rather than prose. AI systems extract tabular data more efficiently than unstructured text. | The AI content audit panel flags comparison content written in prose and suggests converting to a table. User can click "Convert to Table" to restructure. | User says "convert the comparison section to a table" and the system restructures the content into an HTML table on the canvas. |
| 5.17.3.5 | The system must support Speakable schema markup, allowing content sections to be marked as suitable for voice assistant responses. | User selects a content section on the canvas and toggles "Speakable" in the section properties panel. The system generates Speakable schema for marked sections. | User says "mark the summary paragraph as speakable" and the system applies the Speakable schema. User says "which sections on this page are marked as speakable?" and the system lists them. |
| 5.17.3.6 | The system must encourage and support the inclusion of specific data points (numbers, statistics, percentages, dates, prices) in content. | The AI content audit panel shows a data density score per page, counting specific data points. Pages below the recommended threshold are flagged. | User says "how many data points does this page have?" and the system counts and reports. User says "suggest where to add statistics to this page" and the system identifies sections that would benefit from data points. |
| 5.17.3.7 | The system must encourage and support the inclusion of citations to authoritative external sources within content. | The AI content audit panel shows a citation count per page. Pages with no citations are flagged. A citation helper suggests relevant authoritative sources based on the page topic. | User says "add a citation to support the claim in paragraph 3" and the system suggests authoritative sources. User says "how many citations does this page have?" and the system reports. |
| 5.17.3.8 | The system must not apply keyword stuffing or keyword density optimisation. Keyword stuffing has a negative impact on AI search visibility. | The system does not provide keyword density tools or targets. If the AI content audit detects unnatural keyword repetition, it flags a warning. | If a user says "increase the keyword density" the system explains that keyword stuffing harms AI search visibility and suggests natural alternatives. |

#### 5.17.4 Entity & Brand Signals

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.17.4.1 | The system must enforce consistent brand naming across all pages, schema markup, meta tags, and generated files. Brand name fragmentation reduces AI entity recognition. | User configures the canonical brand name in the site settings. The system audits all pages for inconsistencies and flags variations. | User says "check brand name consistency across the site" and the system audits and reports any variations. User says "set our brand name to 'Acme Training International'" and the system updates the canonical reference. |
| 5.17.4.2 | The system must support Organization schema with sameAs properties linking to the brand's social profiles, directory listings, and credential platforms to strengthen entity recognition in AI knowledge graphs. | User opens the Organization schema panel in site settings. Adds sameAs URLs for social profiles, directories, and credential platforms. | User says "add our LinkedIn and Google Business Profile URLs to the Organization schema" and the system updates the sameAs properties. |
| 5.17.4.3 | The system must support author/contributor profiles with Person schema including credentials, experience, and expertise — these serve as E-E-A-T signals that AI systems evaluate when selecting sources to cite. | User creates author profiles with credential fields (per 5.16.2). The system automatically generates Person schema for each author. | User says "does Sarah's author profile have Person schema?" and the system checks and reports. User says "add Sarah's PMP certification to her author schema" and the system updates it. |
| 5.17.4.4 | The system must support E-E-A-T signals throughout the site: visible author credentials, transparent business information, verifiable reviews, clear policies, and trust indicators. | The E-E-A-T audit panel in the SEO dashboard scores the site across Experience, Expertise, Authoritativeness, and Trustworthiness dimensions, flagging gaps. | User says "run an E-E-A-T audit on our site" and the system analyses and reports scores with specific recommendations for improvement. |

#### 5.17.5 AI Content Freshness

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.17.5.1 | The system must display visible "Last updated" dates on all published pages. AI systems use freshness as a citation signal. | System-level — "Last updated" dates are rendered on all published pages automatically. User can configure the date format and placement in the site settings. | User says "where does the last updated date appear on our pages?" and the system shows the placement. User says "move the last updated date to below the title" and the system adjusts the layout. |
| 5.17.5.2 | The system must alert users when published pages have not been updated within a configurable period (default: 90 days). | The content dashboard highlights stale pages with a warning indicator. A staleness report lists all pages past the threshold sorted by age. (Overlaps with 5.16.4 for traditional SEO freshness; this requirement extends it to AI citation impact.) | User says "show me pages that are losing AI citations due to staleness" and the system cross-references freshness data with AI citation trends. |
| 5.17.5.3 | When content is updated, the system must automatically update the page's lastmod date in the XML sitemap, the llms.txt file, and any schema markup containing date information. | System-level — automatic on content save. The SEO dashboard shows lastmod sync status across sitemap, llms.txt, and schema for each page. | System-level — automatic. User says "is the lastmod date in sync across all files for this page?" and the system verifies. |
| 5.17.5.4 | The system must support scheduled content review reminders, prompting users to review and refresh specific pages on a recurring basis. | User opens the content review schedule panel. Sets recurring review dates per page or per content type. Reminders appear in the notification centre (per 5.7.20) when pages are due for review. | User says "remind me to review the pricing page every 60 days" and the system creates the recurring reminder. User says "what pages are due for review this week?" and the system lists them. |

#### 5.17.6 Multi-Platform Awareness

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.17.6.1 | The system must optimise content for multiple AI platforms simultaneously, recognising that each platform selects sources differently: ChatGPT prioritises list articles and directory mentions; Perplexity prioritises factual density and real-time freshness; Google AI Overviews prioritise schema-rich brand-owned content; Claude prioritises well-structured authoritative content. | The AI search optimisation dashboard shows per-platform readiness scores for each page, indicating how well the content is optimised for each major AI platform. | User says "how well is this page optimised for Perplexity?" and the system analyses and reports. User says "what should I change to improve our visibility on ChatGPT?" and the system provides platform-specific recommendations. |
| 5.17.6.2 | The system must support product feed generation and structured data compatible with Google Merchant Center and emerging agentic commerce protocols (Google Universal Commerce Protocol, OpenAI Agentic Commerce Protocol) for e-commerce sites. | User opens the product feed panel. Configures feed format, included products, and update frequency. Views feed validation status and submission history. | User says "generate a product feed for Google Merchant Center" and the system creates it. User says "is our product feed up to date?" and the system checks and reports. |
| 5.17.6.3 | The system must optimise content for voice search by supporting concise answers (20–30 words), conversational phrasing, and Speakable schema markup suitable for voice assistants (Siri, Alexa, Google Assistant). | The voice search readiness panel shows which pages have Speakable markup, concise answer blocks, and conversational headings. Pages missing these are flagged. | User says "is this page optimised for voice search?" and the system analyses and reports. User says "add a voice-friendly answer to the top of this section" and the system generates a concise 20–30 word summary for review. |

### 5.18 Search Monitoring & Measurement

#### 5.18.1 Traditional SEO Monitoring

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.18.1.1 | The system must provide a traditional SEO performance dashboard showing: keyword rankings for tracked terms, organic traffic volume and trends, click-through rates, impressions, index coverage status, Core Web Vitals scores (field data), backlink profile summary, and crawl error reports. | User opens the SEO dashboard from the admin sidebar. Dashboard widgets display each metric with trend charts, period comparisons, and drill-down capability. | User says "show me our SEO dashboard" and the system opens it. User says "how is our organic traffic trending this month?" and the system reports with a comparison to the previous period. |
| 5.18.1.2 | The system must integrate with Google Search Console to pull indexation data, performance metrics, Core Web Vitals field data, and rich result status. | User opens the GSC integration panel. Connects a Google Search Console property. Data syncs automatically and populates the SEO dashboard. | User says "connect Google Search Console" and the system opens the integration setup. User says "what does GSC say about our index coverage?" and the system pulls and displays the data. |
| 5.18.1.3 | The system must integrate with Google Analytics (or equivalent) to track organic traffic, user behaviour, conversions, and referral sources. | User opens the analytics integration panel. Connects a GA4 property (or equivalent). Traffic and behaviour data populate the SEO dashboard and content audit reports. | User says "connect Google Analytics" and the system opens the integration setup. User says "which pages have the highest bounce rate?" and the system queries analytics data and reports. |
| 5.18.1.4 | The system must support local SEO: Google Business Profile integration, local schema markup (LocalBusiness with geo-coordinates), NAP (Name, Address, Phone) consistency enforcement across site pages, and location-specific content pages for multi-location businesses. | User opens the local SEO panel. Connects Google Business Profile. Configures business locations with addresses and geo-coordinates. The system audits NAP consistency across all pages and flags discrepancies. | User says "connect our Google Business Profile" and the system opens the integration. User says "check NAP consistency across the site" and the system audits and reports any mismatches. User says "add a location page for our Sydney office" and the system creates a page template with local schema. |
| 5.18.1.5 | The system must provide SEO recommendations, identifying pages with missing meta descriptions, thin content, broken internal links, missing schema, slow load times, or declining rankings. | The SEO dashboard includes a recommendations panel listing prioritised issues with severity indicators and one-click navigation to the affected page. | User says "what SEO issues should I fix first?" and the system lists prioritised recommendations. User says "show me all pages with missing meta descriptions" and the system lists them. |
| 5.18.1.6 | The system must support content audit functionality: identifying pages with zero organic traffic over a configurable period, pages with high bounce rates, pages with declining rankings, and thin content pages that may be candidates for improvement, consolidation, or removal. | User opens the content audit panel. Configures audit criteria (traffic threshold, time period, bounce rate threshold). The system generates a report categorising pages as healthy, needs improvement, or candidates for removal. | User says "run a content audit" and the system generates the report. User says "which pages have had zero traffic in the last 6 months?" and the system lists them. User says "suggest which pages to consolidate" and the system identifies overlapping content. |

#### 5.18.2 AI Visibility Monitoring

| ID | Requirement | Traditional Interface | AI Interface |
|----|-------------|----------------------|-------------|
| 5.18.2.1 | The system must provide an AI search health dashboard showing: AI-specific schema validation, robots.txt AI crawler status, llms.txt status, and AI search readiness indicators per page. This dashboard may be integrated with the traditional SEO dashboard (per 5.18.1.1). | User opens the AI search dashboard (or an AI tab within the SEO dashboard). Dashboard shows per-page AI readiness scores, crawler access status, llms.txt coverage, and schema validation results. | User says "show me the AI search dashboard" and the system opens it. User says "what's our overall AI search readiness?" and the system reports an aggregate score with per-page breakdowns. |
| 5.18.2.2 | The system must provide AI visibility monitoring, allowing users to track which pages are being cited by AI search engines (ChatGPT, Perplexity, Google AI Overviews, Claude) and track citation trends over time. | The AI visibility panel shows citation counts per page per AI platform, with trend charts over configurable time periods. | User says "which pages are being cited by ChatGPT?" and the system lists them with citation counts. User says "are our AI citations increasing or decreasing?" and the system shows the trend. |
| 5.18.2.3 | The system must track and display AI referral traffic from known AI platforms (chat.openai.com, perplexity.ai, gemini.google.com, claude.ai, copilot.microsoft.com) separately from traditional search traffic. | The AI traffic panel in the analytics dashboard shows referral traffic from each AI platform as a separate channel, with volume, trends, and landing page breakdowns. | User says "how much traffic are we getting from AI platforms?" and the system reports total and per-platform. User says "which pages get the most Perplexity referrals?" and the system lists them. |
| 5.18.2.4 | The system must support prompt-based AI visibility testing, where the user can define target prompts and the system periodically tests them across AI platforms and reports whether the website is cited. | User opens the prompt testing panel. Adds target prompts (e.g., "best project management certification in Australia"). Sets testing frequency and target platforms. Results show citation status per prompt per platform over time. | User says "test whether we appear when someone asks ChatGPT 'best project management certification in Australia'" and the system runs the test and reports. User says "show me all prompt test results" and the system displays the dashboard. |
| 5.18.2.5 | The system must track and display AI-specific metrics: AI Presence Rate (% of relevant queries where the brand appears), Citation Frequency (how often pages are cited), Brand Mention Sentiment (how AI describes the brand), and Share of AI Conversation (brand mentions vs competitors). | The AI metrics panel shows each metric with current values, historical trends, and competitor comparison where available. | User says "what's our AI Presence Rate?" and the system reports. User says "how does AI describe our brand?" and the system shows Brand Mention Sentiment analysis. User says "compare our AI visibility to competitors" and the system displays Share of AI Conversation data. |
